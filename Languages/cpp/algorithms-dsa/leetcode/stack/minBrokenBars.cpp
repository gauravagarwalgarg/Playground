#include <iostream>
#include <vector>
#include <unordered_map>

using namespace std;

int minBrokenBars(vector<vector<int>>& plate) {
  unordered_map<int, int> cutPositions;
  int n = plate.size();

  for(cos
}
